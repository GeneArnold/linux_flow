groq>=1.1.0
pynput>=1.8.0
pystray>=0.19.0
pillow>=10.0.0
sounddevice>=0.4.6
evdev>=1.9.0
# System packages required — see README for distro-specific instructions
# Ubuntu/Debian: xdotool xclip python3-gi python3-gi-cairo gir1.2-adw-1
# Arch Linux:    python-gobject python-cairo gtk4 libadwaita
# Wayland extra: wl-clipboard wtype (+ user must be in 'input' group)
